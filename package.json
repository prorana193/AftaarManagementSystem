{
  "name": "aftaar-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../server && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd server && node server.js & cd frontend && npm start"
  },
  "dependencies": {
    "pdfkit": "^0.17.2"
  }
}
